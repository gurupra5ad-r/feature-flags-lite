{
  "name": "feature-flags-lite",
  "private": true,
  "workspaces": ["app","server"],
  "scripts": {
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix app run dev\"",
    "ci": "npm -ws run test || true"
  },
  "devDependencies": { "concurrently": "^9.0.0" }
}
