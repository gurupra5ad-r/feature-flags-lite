{
  "name": "feature-flags-lite",
  "private": true,
  "workspaces": ["app","server"],
  "scripts": {
    "dev": "concurrently \"yarn workspace server dev\" \"yarn workspace app dev\"",
    "ci": "yarn workspaces foreach -At run test || true"
  },
  "devDependencies": { "concurrently": "^9.0.0" }
}
